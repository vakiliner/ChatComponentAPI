plugins {
	id 'java'
}

allprojects {
	apply plugin: 'java'

	sourceCompatibility = JavaVersion.VERSION_1_8
	targetCompatibility = JavaVersion.VERSION_1_8
}

subprojects {
	apply plugin: 'java'

	archivesBaseName = "${project.archives_base_name}-${project.display_name}"

	jar {
		from '../LICENSE'
	}
}

jar {
	duplicatesStrategy = DuplicatesStrategy.EXCLUDE

	var fabric = project(':fabric').tasks.getByName('jar')
	var forge = project(':forge').tasks.getByName('jar')
	var paper = project(':paper').tasks.getByName('jar')

	from(zipTree(paper.archiveFile)) {
		exclude 'LICENSE'
	}
	from(zipTree(fabric.archiveFile)) {
		exclude 'LICENSE'
	}
	from(zipTree(forge.archiveFile)) {
		exclude 'LICENSE'
	}

	manifest {
		from paper.manifest
		from fabric.manifest
		from forge.manifest
	}
}
