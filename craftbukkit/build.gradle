plugins {
	id 'java'
}

dependencies {
	implementation project(':base')
}

jar {
	var task = project(':bukkit').tasks.getByName('jar')

	from(zipTree(task.archiveFile)) {
		exclude 'LICENSE'
	}

	manifest {
		from task.manifest
	}
}
