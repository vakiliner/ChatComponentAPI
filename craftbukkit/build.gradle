plugins {
	id 'application'
}

dependencies {
	implementation project(':base')
	compileOnly fileTree(include: ['*.jar'], dir: 'libs')
}

compileJava {
	source project(':bukkit').tasks.named('compileJava')
}

processResources {
	from project(':bukkit').tasks.named('processResources')
}